# --- keep control files at repo root ---
!.gitignore
!README.md
!LICENSE
!LICENSE-docs.md

# --- ignore everything at repo root by default ---
/*

# --- but keep this directory (and thus its contents) ---
!documents/

# (optional) if you want to be explicit inside the allowed dir:
# !documents/**
# You generally don't need the line above once the dir is un-ignored.

# --- common niceties anywhere ---
**/.DS_Store
